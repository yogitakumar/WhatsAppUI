import 'package:flutter/material.dart';

class camera extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Camera",style: TextStyle(fontSize: 20.0),),
    );
  }
}
